import type { Metadata } from "next";

export const metadata: Metadata = { title: "Detection" };

export default function DetectionLayout({ children }: { children: React.ReactNode }) {
  return children;
}
