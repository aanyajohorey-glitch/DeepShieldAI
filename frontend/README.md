# DeepShield AI — Frontend

This is the Next.js frontend for DeepShield AI. For the full project
overview, architecture, setup instructions, and deployment guide, see the
[root README](../README.md).

## Quick start

```bash
npm install
copy .env.local.example .env.local
npm run dev
```

App runs at `http://localhost:3000`.
